# Testing rules

- Every behavior change requires validation at the right layer.
- Bug fixes should add regression coverage when practical.
- Prefer targeted, high-signal tests first; add broader tests when integration risk is meaningful.
- Verify failure paths, not just success paths.
- Never claim success based only on reasoning when tests or deterministic checks are available.
